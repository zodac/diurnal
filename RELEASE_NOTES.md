## Minor Update

Added a new environment variable `NOTE_MAX_LENGTH` to define the maximum length (in characters) for a note. Lowering this value from the default of **10,000** will not remove existing notes, simply prevent a new save.
