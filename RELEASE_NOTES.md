## Backwards Incompatible Change

There should be no need to migrate any data, it is a change to the default values of an environment variable.

- Changing the default value of `APP_UPDATE_CHECK_ENABLED` to false, so any external communication is explicitly opt-in
