# Copy to .env and fill in values before running docker-compose

# Session
SESSION_ENCRYPTION_KEY=change-me-to-a-random-32-char-string

# Database
DB_NAME=lifetracker
DB_USER=lifetracker
DB_PASSWORD=change-me-strong-password

# Application port (host)
APP_PORT=8080

# OIDC — Phase 2, leave OIDC_ENABLED=false to use password auth only
OIDC_ENABLED=false
OIDC_ISSUER_URL=https://auth.example.com
OIDC_CLIENT_ID=life-tracker
OIDC_CLIENT_SECRET=
