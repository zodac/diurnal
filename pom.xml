<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>dev.lifetracker</groupId>
    <artifactId>life-tracker-api</artifactId>
    <version>1.0.0-SNAPSHOT</version>

    <properties>
        <maven.compiler.release>26</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <quarkus.platform.version>3.36.1</quarkus.platform.version>
        <compiler-plugin.version>3.13.0</compiler-plugin.version>
        <surefire-plugin.version>3.3.1</surefire-plugin.version>
        <!-- Single "testing" HTTP port, shared by both test tiers: the *IT tests bind it as the
             @QuarkusTest test-port (Quarkus default 8081), and the packaged-jar E2E run binds the
             same port. They never clash because they don't overlap in time — under -Dall the E2E
             jar is started in the integration-test phase *after* failsafe has finished the *IT
             tests (and released the port), not before (see the exec executions below). 8081 is
             distinct from 8080 (the production container), so -Dall coexists with a deployment. -->
        <e2e.http.port>8081</e2e.http.port>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>io.quarkus.platform</groupId>
                <artifactId>quarkus-bom</artifactId>
                <version>${quarkus.platform.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <!-- REST -->
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-rest-jackson</artifactId>
        </dependency>
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-hibernate-validator</artifactId>
        </dependency>

        <!-- Database -->
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-hibernate-orm-panache</artifactId>
        </dependency>
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-jdbc-postgresql</artifactId>
        </dependency>
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-flyway</artifactId>
        </dependency>

        <!-- Server-side templating -->
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-rest-qute</artifactId>
        </dependency>

        <!-- JWT (verification + generation) — kept for REST API / future OIDC exchange -->
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-smallrye-jwt</artifactId>
        </dependency>
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-smallrye-jwt-build</artifactId>
        </dependency>

        <!-- Password hashing -->
        <dependency>
            <groupId>org.mindrot</groupId>
            <artifactId>jbcrypt</artifactId>
            <version>0.4</version>
        </dependency>

        <!-- OpenAPI / Dev UI -->
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-smallrye-openapi</artifactId>
        </dependency>

        <!-- OIDC: active at runtime only when OIDC_ENABLED=true; harmless otherwise -->
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-oidc</artifactId>
        </dependency>

        <!-- Testing -->
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-junit</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>io.rest-assured</groupId>
            <artifactId>rest-assured</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-test-security</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>io.quarkus.platform</groupId>
                <artifactId>quarkus-maven-plugin</artifactId>
                <version>${quarkus.platform.version}</version>
                <extensions>true</extensions>
                <executions>
                    <execution>
                        <goals>
                            <goal>build</goal>
                            <goal>generate-code</goal>
                            <goal>generate-code-tests</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>${compiler-plugin.version}</version>
                <configuration>
                    <compilerArgs>
                        <arg>-parameters</arg>
                    </compilerArgs>
                </configuration>
            </plugin>
            <plugin>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>${surefire-plugin.version}</version>
                <configuration>
                    <!-- Default: unit tests only. Activate -Dit or -Dall to include *IT tests. -->
                    <includes>
                        <include>**/*Test.java</include>
                    </includes>
                    <systemPropertyVariables>
                        <java.util.logging.manager>org.jboss.logmanager.LogManager</java.util.logging.manager>
                        <maven.home>${maven.home}</maven.home>
                        <net.bytebuddy.experimental>true</net.bytebuddy.experimental>
                    </systemPropertyVariables>
                </configuration>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <!--
            Run integration tests only — starts and stops the test DB automatically:
                mvn clean install -Dit
        -->
        <profile>
            <id>it</id>
            <activation>
                <property>
                    <name>it</name>
                </property>
            </activation>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.codehaus.mojo</groupId>
                        <artifactId>exec-maven-plugin</artifactId>
                        <version>3.3.0</version>
                        <executions>
                            <execution>
                                <id>docker-db-start</id>
                                <phase>pre-integration-test</phase>
                                <goals><goal>exec</goal></goals>
                                <configuration>
                                    <executable>docker</executable>
                                    <arguments>
                                        <argument>compose</argument>
                                        <argument>-f</argument>
                                        <argument>${project.basedir}/docker-compose.dev.yml</argument>
                                        <argument>up</argument>
                                        <argument>-d</argument>
                                        <argument>--wait</argument>
                                        <argument>test-db</argument>
                                    </arguments>
                                </configuration>
                            </execution>
                            <execution>
                                <id>docker-db-stop</id>
                                <phase>post-integration-test</phase>
                                <goals><goal>exec</goal></goals>
                                <configuration>
                                    <executable>docker</executable>
                                    <arguments>
                                        <argument>compose</argument>
                                        <argument>-f</argument>
                                        <argument>${project.basedir}/docker-compose.dev.yml</argument>
                                        <argument>rm</argument>
                                        <argument>-s</argument>
                                        <argument>-f</argument>
                                        <argument>test-db</argument>
                                    </arguments>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                    <!--
                        Failsafe (not surefire) defers build failure to the verify phase,
                        so post-integration-test teardown always runs even when tests fail.
                    -->
                    <plugin>
                        <artifactId>maven-failsafe-plugin</artifactId>
                        <version>${surefire-plugin.version}</version>
                        <executions>
                            <execution>
                                <goals>
                                    <goal>integration-test</goal>
                                    <goal>verify</goal>
                                </goals>
                                <configuration>
                                    <includes>
                                        <include>**/*IT.java</include>
                                    </includes>
                                    <systemPropertyVariables>
                                        <java.util.logging.manager>org.jboss.logmanager.LogManager</java.util.logging.manager>
                                        <maven.home>${maven.home}</maven.home>
                                        <net.bytebuddy.experimental>true</net.bytebuddy.experimental>
                                    </systemPropertyVariables>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>

        <!--
            Run ALL test tiers in one command — starts and stops the test DB automatically:
                mvn clean install -Dall

            Phase sequence:
              test                – surefire: unit tests (*Test) only
              package             – Quarkus builds the fast-jar
              pre-integration-test – docker compose up (DB) only
              integration-test    – failsafe: *IT tests (@QuarkusTest on 8081) run FIRST; then the
                                     packaged jar starts on the same 8081 (now free) + Playwright E2E
                                     (results captured; clean-up always runs). Ordering is guaranteed
                                     by failsafe being declared before exec-maven-plugin.
              post-integration-test – kill app by PID + docker compose down (always runs; failsafe defers failure)
              verify              – failsafe reports IT failures + playwright-check reports E2E failures

            Both tiers share ONE testing port (8081, the @QuarkusTest default); they never overlap
            in time, so a single port suffices and -Dall stays clear of the 8080 production container.
        -->
        <profile>
            <id>all</id>
            <activation>
                <property>
                    <name>all</name>
                </property>
            </activation>
            <build>
                <plugins>
                    <!-- Unit tests only; IT tests run via failsafe in integration-test phase -->
                    <plugin>
                        <artifactId>maven-surefire-plugin</artifactId>
                        <configuration>
                            <includes combine.children="override">
                                <include>**/*Test.java</include>
                            </includes>
                        </configuration>
                    </plugin>
                    <!--
                        Failsafe defers build failure to verify, so post-integration-test
                        teardown always runs even when IT tests fail.
                    -->
                    <plugin>
                        <artifactId>maven-failsafe-plugin</artifactId>
                        <version>${surefire-plugin.version}</version>
                        <executions>
                            <execution>
                                <goals>
                                    <goal>integration-test</goal>
                                    <goal>verify</goal>
                                </goals>
                                <configuration>
                                    <includes>
                                        <include>**/*IT.java</include>
                                    </includes>
                                    <systemPropertyVariables>
                                        <java.util.logging.manager>org.jboss.logmanager.LogManager</java.util.logging.manager>
                                        <maven.home>${maven.home}</maven.home>
                                        <net.bytebuddy.experimental>true</net.bytebuddy.experimental>
                                    </systemPropertyVariables>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                    <plugin>
                        <groupId>org.codehaus.mojo</groupId>
                        <artifactId>exec-maven-plugin</artifactId>
                        <version>3.3.0</version>
                        <executions>

                            <!-- 1. Start the test DB and block until its healthcheck passes -->
                            <execution>
                                <id>docker-db-start</id>
                                <phase>pre-integration-test</phase>
                                <goals><goal>exec</goal></goals>
                                <configuration>
                                    <executable>docker</executable>
                                    <arguments>
                                        <argument>compose</argument>
                                        <argument>-f</argument>
                                        <argument>${project.basedir}/docker-compose.dev.yml</argument>
                                        <argument>up</argument>
                                        <argument>-d</argument>
                                        <argument>--wait</argument>
                                        <argument>test-db</argument>
                                    </arguments>
                                </configuration>
                            </execution>

                            <!-- 2. Start Quarkus in test profile; poll /login until ready (max 120s).
                                 Bound to integration-test (NOT pre-integration-test) and this exec
                                 plugin is declared after maven-failsafe-plugin, so it runs only once
                                 failsafe has finished the *IT tests — by then the @QuarkusTest test
                                 instance has released ${e2e.http.port}, which this jar then reuses. -->
                            <execution>
                                <id>quarkus-start-e2e</id>
                                <phase>integration-test</phase>
                                <goals><goal>exec</goal></goals>
                                <configuration>
                                    <executable>bash</executable>
                                    <arguments>
                                        <argument>-c</argument>
                                        <argument>java -Dquarkus.profile=test -Dquarkus.http.port=${e2e.http.port} -jar "${project.build.directory}/quarkus-app/quarkus-run.jar" &gt;"${project.build.directory}/app.log" 2&gt;&amp;1 &amp; echo $! &gt;"${project.build.directory}/app.pid"; for i in $(seq 1 60); do curl -sf http://localhost:${e2e.http.port}/login &gt;/dev/null 2&gt;&amp;1 &amp;&amp; echo "App ready" &amp;&amp; exit 0; sleep 2; done; echo "App failed to start — see target/app.log"; exit 1</argument>
                                    </arguments>
                                </configuration>
                            </execution>

                            <!--
                                3. Run Playwright E2E tests.
                                Exits 0 regardless of test outcome so post-integration-test
                                clean-up always runs; the real result is checked in verify.
                            -->
                            <execution>
                                <id>playwright-run</id>
                                <phase>integration-test</phase>
                                <goals><goal>exec</goal></goals>
                                <configuration>
                                    <executable>bash</executable>
                                    <arguments>
                                        <argument>-c</argument>
                                        <argument>cd "${project.basedir}/e2e" &amp;&amp; npm test; rc=$?; echo $rc &gt; "${project.build.directory}/playwright-result.txt"; exit 0</argument>
                                    </arguments>
                                    <environmentVariables>
                                        <BASE_URL>http://localhost:${e2e.http.port}</BASE_URL>
                                    </environmentVariables>
                                </configuration>
                            </execution>

                            <!-- 4. Stop Quarkus (runs even when tests failed) -->
                            <execution>
                                <id>quarkus-stop-e2e</id>
                                <phase>post-integration-test</phase>
                                <goals><goal>exec</goal></goals>
                                <configuration>
                                    <executable>bash</executable>
                                    <arguments>
                                        <argument>-c</argument>
                                        <argument>kill -9 $(cat "${project.build.directory}/app.pid" 2&gt;/dev/null) 2&gt;/dev/null || true</argument>
                                    </arguments>
                                </configuration>
                            </execution>

                            <!-- 5. Tear down the test DB (runs even when tests failed) -->
                            <execution>
                                <id>docker-db-stop</id>
                                <phase>post-integration-test</phase>
                                <goals><goal>exec</goal></goals>
                                <configuration>
                                    <executable>docker</executable>
                                    <arguments>
                                        <argument>compose</argument>
                                        <argument>-f</argument>
                                        <argument>${project.basedir}/docker-compose.dev.yml</argument>
                                        <argument>rm</argument>
                                        <argument>-s</argument>
                                        <argument>-f</argument>
                                        <argument>test-db</argument>
                                    </arguments>
                                </configuration>
                            </execution>

                            <!-- 6. Fail the build if Playwright reported a non-zero exit code -->
                            <execution>
                                <id>playwright-check</id>
                                <phase>verify</phase>
                                <goals><goal>exec</goal></goals>
                                <configuration>
                                    <executable>bash</executable>
                                    <arguments>
                                        <argument>-c</argument>
                                        <argument>rc=$(cat "${project.build.directory}/playwright-result.txt" 2>/dev/null || echo 1); [ "$rc" = "0" ] || { echo "Playwright E2E tests FAILED (exit $rc) — see e2e/playwright-report/"; exit 1; }</argument>
                                    </arguments>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>
</project>
